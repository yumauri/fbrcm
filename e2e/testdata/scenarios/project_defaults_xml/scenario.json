{
  "name": "project_defaults_xml",
  "command_id": "project.defaults",
  "args": [
    "project",
    "defaults",
    "${PROJECT_ID}",
    "--format",
    "xml"
  ],
  "expected_exit_code": 0,
  "expected_http": [
    {
      "method": "GET",
      "host": "firebaseremoteconfig.googleapis.com",
      "path": "/v1/projects/${PROJECT_ID}/remoteConfig:downloadDefaults",
      "status": 200
    }
  ]
}
