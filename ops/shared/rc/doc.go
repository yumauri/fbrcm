// Package rc implements the CLI Remote Config pipeline:
// input extraction, order-preserving JSON, diff rendering, export normalization,
// and validate/publish with ETag conflict retry.
package rc
